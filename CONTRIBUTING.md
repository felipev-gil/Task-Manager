# Contributing
Create a focused branch for one feature or fix. Use commits that explain the behavior,
for example `fix(auth): recover from an expired session`.
Run the checks in README before opening a pull request. Explain the problem, resulting
behavior, and validation evidence. Keep credentials and local database files out of commits.
Historical commits are preserved; future changes should make individual decisions reviewable.
